---
interface Props {
  section: string;
}
const { section } = Astro.props;
---
<div class="status-bar" role="status" aria-label="editor status">
  <span>● main</span>
  <span>UTF-8</span>
  <span style="margin-left: auto">{section}</span>
</div>
