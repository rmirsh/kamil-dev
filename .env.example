# Optional — raises GitHub API rate limit from 60 to 5000 req/hr at build time
GITHUB_TOKEN=

# Telegram → Blog sync
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=   # any random string, set same in Vercel env vars
